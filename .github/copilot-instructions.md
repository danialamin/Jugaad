# Copilot Instructions

## Project Guidelines
- User's batch scripts must verify remote branches and output verbose git status when debugging why commits/pushes are failing (e.g. branch names, untracked files).
- Ignore the Visual Studio (.vs) folder entirely in the Git workflow.